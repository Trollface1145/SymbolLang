# ~!@#$%

> 始于 Word，不止于 Word

**~!@#$%**（别名 **X X XXX**）是一门基于栈的符号驱动型编程语言，由 **Trollface1145** 于 2026 年暑假设计并实现。  
它通过符号形状暗示操作含义，所有指令以 `@` 开头，表示“当前册（作用域）的操作”。语言面向有编程基础的用户，同时通过“方言”机制降低初学者的学习门槛。

---

##  语言特性

- **符号指令**：所有操作以 `@` 开头，简洁一致  
- **栈操作**：支持多栈，可显式指定目标栈  
- **变量与类型**：支持整型、浮点型、布尔型（0.4.2 起）  
- **指针**：支持指针创建、赋值、解引用（0.4.3 起）  
- **控制流**：`if`、`else`、`while`、`for`  
- **函数**：支持参数传递与返回值  
- **输入输出**：`@>` 输出，`@<` 输入  
- **方言机制**：支持中文方言（`#<chinese_library>`[*1]），降低学习门槛  
- **跨平台**：C++ 实现，可编译为独立 `.exe` 运行

---

##  快速开始

### 1. 编译解释器

确保你的系统已安装 **C++17 编译器**（如 MinGW、MSVC 或 Clang）。

`g++ -std=c++17 -o interpreter.exe interpreter.cpp`

或使用 MSVC：

`cl /EHsc /std:c++17 interpreter.cpp /Fe:interpreter.exe`

----
### 2. 编写你的第一个程序
创建一个文本文件，扩展名为 `.~`（例如 `hello.~`），内容如下：

----
@! X
`
@> "Hello, World!"
`
@&& X

----

### 3. 运行程序
`interpreter.exe hello.~`

#### 语法概览
指令	        功能	            示例
`@!栈名`	     创建栈	            `@! X`
`@*~ 值<变量`	压栈并绑定变量	     `@*~ 10<a`
`@!!~ 表达式`	计算表达式，压栈	 `@!!~ a+b`
`@&& 栈名`	    输出栈顶	        `@&& X`
`@>"文本"`	    输出文本或表达式	`@>"计算结果："`
`@<"提示"<变量`	输入数字	        `@<"请输入"<n`
`@# 变量=值`	声明布尔变量	    `@# flag=*#`
`!!@ 指针名`	创建指针	        `!!@ p`
`@& 指针名<变量`指针指向变量	     `@& p<a`
`@* 指针名`	    解引用指针	        `@> @* p`
`*#` / `/#`	   布尔真/假	       `@# ready=*#`
`[/* ... */]`	注释	           `[/* 这是注释 */]`

#### 控制流：

----
/ [条件] { ... }        // if
(**) { ... }           // else
-/* [条件] { ... }     // while
(/) [变量<次数] { ... } // for

----

#### 函数：
`*@'函数名'(参数) { ... }*@> 返回值`

#### 项目结构
~!@#$%/
├── interpreter.cpp      # 解释器核心源码
├── ide.bat              # 轻量级 IDE（仅 Windows）
├── examples/            # 示例程序
│   ├── hello.~          # 示例代码文件
├── LICENSE              # 版权声明
└── README.md            # 本文件

#### 版本历史
版本	日期	新增特性
0.1	2026-08-17	基础栈操作、控制流、输入输出
0.2	2026-08-18	修女主题整活输出
0.3	2026-08-19	函数定义与调用
0.4.1	2026-08-22	歧义语法修复、多栈支持、返回值统一
0.4.2	2026-08-25	布尔型变量
0.4.3	2026-08-25	指针操作
0.4.4	待定	综合修复
1.0	待定	正式版
#### 贡献与致谢
作者：Trollface_巨魔（QQ: 2166439786）

特别感谢：DeepSeek 在技术文档与代码调试中的协助

版权：© 2014-2026 Creative Gears. All rights reserved.

#### 许可证
本项目采用 Creative Gears 专属许可证，保留所有权利。
未经作者书面许可，不得用于商业用途。

#### 星标
如果你喜欢这个项目，欢迎在 GitHub 上点个 Star，这将是对一个 12 岁开发者最大的鼓励！

*始于 Word，不止于 Word*
—— 2026 年 8 月 12 日，一个准初中生用一整个暑假，让创意变成了现实。
本文件编写于2026/08/25(第一次用MarkDown写示例文件，写的不好，见谅)

----
#### 附录(ps):
*1:目前只是处于规划阶段，见谅